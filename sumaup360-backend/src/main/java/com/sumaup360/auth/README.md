# auth (schema: auth)

Identidad efectiva. Vincula el usuario de Firebase con el usuario del backend, emite la
sesión efectiva (claims de rol/permiso/tenant) y expone login/refresh. El backend es la
única autoridad de autorización.
