package com.sumaup360.app.honorarios.enums;

/** Estados de una solicitud de Recibo por Honorarios. */
public enum HonorarioStatus {
    PENDIENTE,
    EN_PROCESO,
    GENERADO,
    OBSERVADO,
    CANCELADO
}
