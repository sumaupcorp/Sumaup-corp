package com.sumaup360.app.honorarios.enums;

/** Estados de una solicitud de Suspension de 4ta categoria (anual). */
public enum SuspensionStatus {
    SOLICITADA,
    EN_PROCESO,
    TRAMITADA,
    OBSERVADA,
    CANCELADA
}
