package com.sumaup360.app.enums;

/** Estado del recibo. El procesamiento lo realiza el Backoffice (contadores). */
public enum ReceiptStatus {
    PENDING,
    IN_PROCESS,
    PROCESSED,
    OBSERVED
}
