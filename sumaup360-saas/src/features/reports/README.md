# features/reports — Reportes y KPIs

Reportes y tableros de indicadores (ventas, inventario, caja) usando componentes de `charts/`
(Recharts) y `tables/`. Genéricos y parametrizados por módulo. Los datos y agregaciones vienen
del backend; el frontend solo visualiza. Estados de carga / error / vacío obligatorios.
