# features/permissions — Permisos

Lectura de los permisos efectivos del usuario (`recurso:accion`) para gatear la UI: ocultar o
deshabilitar acciones que el usuario no puede ejecutar. Esto es UX, **no** seguridad: el
backend reevalúa cada acción. Ver skill `saas-rbac-gating`.
