# features/companies — Empresas

Datos de la empresa dentro del tenant: nombre, RUC, `businessType` y `vertical` (que
determinan los módulos por defecto). Vistas y formularios de configuración de empresa. El
backend valida cambios y recalcula `enabledModules`.
