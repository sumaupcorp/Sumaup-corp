# core/network/
Cliente Dio compartido + interceptor de autenticacion que inyecta el header Authorization: Bearer <idToken> en cada request. Maneja expiracion/refresh del token. Ver skill flutter-dio-auth.
